/target
/dist

# Astro docs site build output, generated types, and dependencies
docs-site/dist/
docs-site/.astro/
docs-site/node_modules/

# poly (polylint) local cache
/.polylint
# poly local hook-channel preferences (per-machine; not shared)
poly.local.toml

# downloaded ONNX model cache (default location is ~/.cache/sceptre, but keep
# any in-tree cache out of git as well)
/.cache
*.onnx

*.swp
.env

# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# local model export artifacts
*.safetensors
*.pth

# Raw comparative benchmark output (machine- and load-specific; reproduce via
# `task python:benchmark`). The distilled, published numbers ARE committed, under
# benchmarks/published/ — see ADR 0030.
/benchmark-results

# BEGIN ai-rulez (DO NOT EDIT - managed by ai-rulez)
.ai-rulez/.generated-manifest.json
.claude/
.codex/
.mcp.json
AGENTS.md
CLAUDE.md
# END ai-rulez
